# Build and installation outputs
build/
install/
dist/
.brew-x86/
reference

# Microsoft redistributables. Purged from history and no longer tracked: they
# are not ours to redistribute. The local copy stays here as a reference set
# for config/redist-manifest.json; interactive_setup.py fetches the same files
# from the public vendor installers at wrapper-setup time. Cold-stored in
# gamma-wip under archive/removed-from-history/.
runtime/redist/

# Apple Game Porting Toolkit payloads. Purged from history for the same
# reason; D3DMetal is not redistributable.
renderers/gptk40b1/
renderers/gptk40b2/

# Local AI-assistant session metadata
.claude-launcher/

# macOS metadata
.DS_Store
*.swp
*~

# Python
__pycache__/